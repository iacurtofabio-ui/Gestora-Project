Fix emersi durante i test della checklist fase 5

Pannello Admin Utenti:

1. Al cambio password deve essere richiesto prima di inserire quella attuale altrimenti non è possibile modificarla.
   -- Verificato: "Reset password" nel pannello Admin è per design senza vecchia password (è l'Admin che sblocca un altro utente, non un cambio password di se stessi). Deciso con Fabio il 26/08/2026 di chiudere il punto così com'è, nessuna modifica applicata.

Ruolo Cliente:

2. Nella pagina Prenotazioni (unica pagina visibile correttamente) compare: "Non hai i permessi per visualizzare questa sezione. Contatta l'amministratore." -- https://gestora-project-production.up.railway.app/api/Prenotazione/get-all-prenotazioni?pageSize=100 -- 403 Forbidden
   -- FIX applicato (26/08/2026): usePrenotazioni.ts chiamava sempre get-all-prenotazioni (solo Admin/Staff). Ora chiama get-mie-prenotazioni quando l'utente non è Admin/Staff.

3. Non è presente un bottone Crea una nuova prenotazione come Cliente.
   -- FIX applicato (26/08/2026): il bottone "+ Aggiungi" esisteva già per tutti i ruoli, ma la pagina non arrivava a renderizzarlo per via del 403 del punto 2. Risolto insieme al punto 2.

Ruolo Staff:

4. l'utente Staff vede i pulsanti crea/modifica/elimina su Zone/Postazioni/Fasce, quando non dovrebbe vederli e alla creazione o modifica si genera un errore ma non è il comportamento previsto. L'utente Staff non deve vedere i pulsanti crea/modifica/elimina su Zone/Postazioni/Fasce.
   -- FIX applicato (26/08/2026): ZonePage.tsx, PostazionePage.tsx, FasciaOrariaPage.tsx mostravano i pulsanti a chiunque. Ora "+ Aggiungi"/"Modifica"/"Elimina" e la colonna Azioni sono visibili solo con ruolo Admin.

Prenotazioni:

5. utente staff o admin non devono avere il blocco di creare una sola prenotazione al giorno ma devono poter aggiungere diverse prenotazioni altrimenti se devono inserire quelle prese ad esempio telefonicamente non possono.
   -- FIX applicato (26/08/2026): il vincolo era un indice univoco DB su (UserId, Data) — bloccava Staff/Admin perché la prenotazione per un cliente telefonico veniva registrata sotto il loro UserId. Rimosso l'indice (migration RimuoviVincoloIndiceAggiungiNomeCliente), il controllo ora vive in PrenotazioniService e si applica solo quando chi crea la prenotazione è Cliente (self-service). ATTENZIONE: la migration va applicata a mano sul DB Railway di produzione (stesso procedimento di FIX-009, tunnel Railway CLI + psql) prima che il fix sia effettivo online.
5. Aggiungere il nome alla prenotazione altrimenti in questo modo non identifica.
   -- FIX applicato (26/08/2026): aggiunto campo NomeCliente (facoltativo) a Prenotazione — visibile nel form di creazione solo per Staff/Admin, mostrato in tabella al posto del nome utente quando valorizzato. Stessa migration del punto sopra.

Punti in sospeso (segnalati in sessioni precedenti, backlog non ancora chiuso):

6. Non esiste nessuna UI per creare un nuovo utente: il backend ha l'endpoint di registrazione ma non è richiamato da nessuna pagina del frontend (né una pagina di registrazione pubblica né un bottone "Crea utente" nel pannello Admin). In produzione l'unico modo per creare un cliente/staff è passare da Postman.
   -- FIX applicato (27/08/2026): entrambe le opzioni implementate (decisione Fabio). Pagina pubblica /register (linkata dal Login) per l'auto-registrazione Cliente. Bottone "+ Crea utente" nel pannello Admin (compone register + assign-role/remove-role, dato che il backend non ha un endpoint dedicato "crea con ruolo").

7. Il Cliente non può modificare né annullare da solo le proprie prenotazioni (tolto il 13/08/2026) — va progettata una regola di cutoff (es. annullabile solo fino a N ore prima dell'orario) prima di riaprire la funzione.
   -- FIX applicato (27/08/2026): cutoff di 2 ore prima dell'orario prenotato (decisione Fabio). Oltre la soglia l'azione è bloccata del tutto, nessuna approvazione Staff. Il vincolo vale solo per il ruolo Cliente in self-service, non per Admin/Staff. update-prenotazione e annulla-prenotazione riaperte al Cliente.

8. Nessuna azione (creazione/modifica/eliminazione di zone, postazioni, fasce, prenotazioni) registra quale utente l'ha effettuata.
   -- FIX applicato (27/08/2026): esteso il servizio di log attività (già usato per login/registrazione) a Zone, Postazioni, Fasce Orarie — stesso pattern già presente su Prenotazioni. Nessuna nuova tabella, riuso di infrastruttura esistente.

9. Il file Controllers/FasciaOrariaController.cs contiene la classe FasceOrarieController — nome del file disallineato dal nome della classe, da rinominare.
   -- FIX applicato (27/08/2026): file rinominato in FasceOrarieController.cs con git mv. Zero impatto sulla route (deriva dal nome della classe, invariata).

10. (nuovo, trovato mentre si chiudevano i punti sopra) GetPostazioniDisponibiliAsync aveva lo stesso filtro troppo restrittivo già corretto sulle postazioni per zona (punto 5), ma su un endpoint non ancora collegato al frontend — quindi innocuo, corretto comunque in via preventiva.
   -- FIX applicato (27/08/2026): rimosso lo stesso filtro "mai prenotata" da GetPostazioniDisponibiliAsync.

Tutti i punti di Fase 5 sono chiusi — dotnet test 31/31 verdi, nessun backlog residuo prima della Fase 6.




